services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_BASE_URL=https://api.resonix.audio
    container_name: resonix-web
    ports:
      - "3000:3000"
    volumes:
      - ./waitlist.json:/app/waitlist.json
    restart: unless-stopped
